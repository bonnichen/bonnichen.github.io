import "./style.css";

const ViewProjectButton = () => {
  return (
    <div>
      <button className="viewProjectButton">View My Projects</button>
    </div>
  );
};

export default ViewProjectButton;
